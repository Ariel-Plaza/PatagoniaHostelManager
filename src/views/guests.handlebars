<h2 class="text-center py-3">Guest List</h2>
            <table class="table tableUsuarios">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Name</th>
                        <th scope="col">Last Name</th>
                        <th scope="col">Nationality</th>
                        <th scope="col">Email</th>
                        {{!-- <th scope="col">Phone Number</th>
                        <th scope="col">Street</th>
                        <th scope="col">City</th>
                        <th scope="col">Country</th> --}}

                      <th scope="col">Detalles</th>
                        <th scope="col">Modificar</th>
                        <th scope="col">Eliminar</th>
                    </tr>
                </thead>
                <tbody>
                    {{!-- //recorre el objeto y captura la info segun el campo para mostrar --}}
                    {{#each guests}}
                        <tr>
                            <th scope="row">{{this.guest_id}}</th>
                            <td>{{this.first_name}}</td>
                            <td>{{this.last_name}}</td>
                            <td>{{this.nationality}}</td>
                            <td>{{this.email}}</td>
                            {{!-- <td>{{this.phone_number}}</td>
                            <td>{{this.Address.street}}</td>
                            <td>{{this.Address.city}}</td> --}}
                            <td>{{this.Address.country}}</td>


                            {{!-- LINK A VISTA DETALLES --}}
                            {{!-- <td><a href="/detalles/usuario/{{this.id}}" class="btn btn-info">Ver detalles</a></td> --}}
                            {{!-- BOTON MODIFICAR QUE CAPTUA DATOS PARA ACTUALIZAR --}}
                            {{!-- <td><button class="btn btn-warning"
                            data-id="{{this.id}}"
                            data-nombre="{{this.nombre}}"
                            data-apellido="{{this.apellido}}"
                            data-email="{{this.email}}"
                            data-imagen="{{this.imagen}}"
                            data-direccion="{{this.direccion.direccion}}"
                            data-comuna="{{this.direccion.comuna}}"
                            data-ciudad="{{this.direccion.ciudad}}"

                            data-bs-toggle="modal" data-bs-target="#updateModal"
                            >Modificar</button></td> --}}
                            {{!-- BOTON ELIMINAR POR ID --}}
                            {{!-- <td><button class="btn btn-danger" data-id="{{this.id}}">Eliminar</button></td> --}}
                        </tr>
                    {{/each }}
                </tbody>
            </table>